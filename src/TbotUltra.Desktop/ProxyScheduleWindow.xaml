<Window x:Class="TbotUltra.Desktop.ProxyScheduleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Proxy schedule"
        Width="1220"
        Height="620"
        MinWidth="980"
        MinHeight="520"
        Background="{DynamicResource AppBackgroundBrush}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="210" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Proxy rotation" FontSize="18" FontWeight="SemiBold" />
                <TextBlock Text="Allowed hours from Settings are shown first. Add one row per proxy and check the hours that proxy should use."
                           Margin="0,4,16,0" TextWrapping="Wrap" Foreground="{DynamicResource SlateTextBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Schedule variation (%)" FontWeight="SemiBold" />
                <TextBox x:Name="VariationTextBox" Margin="0,4,0,0" TextChanged="VariationTextBox_TextChanged" />
                <TextBlock Text="0–49%. 30% = up to ±18 minutes." FontSize="11" Foreground="{DynamicResource SlateTextBrush}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DockPanel Margin="0,0,0,8">
                    <Button Content="Add proxy" Width="100" DockPanel.Dock="Left" Click="AddProxyButton_Click" />
                    <TextBlock Text="Blue/checked hours are active. Each hour may belong to only one proxy for the same day group."
                               Margin="12,4,0,0" Foreground="{DynamicResource SlateTextBrush}" />
                </DockPanel>
                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Grid x:Name="TimelineGrid" />
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Row="2" Margin="0,12,0,0" Padding="10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5"
                Background="{DynamicResource ControlBackgroundBrush}">
            <TextBlock x:Name="ValidationTextBlock" Text="Add proxies and press Validate setup." TextWrapping="Wrap" />
        </Border>

        <DockPanel Grid.Row="3" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button x:Name="ValidateButton" Content="Validate setup" Width="120" Click="ValidateButton_Click" />
                <Button Content="Save draft" Width="100" Margin="8,0,0,0" Click="SaveDraftButton_Click" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ActivateButton" Content="Use setup" Width="100" Click="ActivateButton_Click" />
                <Button Content="Cancel" Width="90" Margin="8,0,0,0" Click="CancelButton_Click" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
