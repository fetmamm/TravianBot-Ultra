<Window x:Class="TbotUltra.Desktop.DashboardFunctionListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loop Task List"
        Height="420"
        Width="380"
        MinHeight="340"
        MinWidth="340"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Select loop tasks to show and run"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#111827"
                   Margin="0,0,0,10" />

        <Border Grid.Row="1"
                BorderBrush="#D1D5DB"
                BorderThickness="1"
                CornerRadius="8"
                Background="#FFFFFF"
                Padding="8">
            <ListBox x:Name="FunctionOptionsListBox"
                     BorderThickness="0"
                     Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Label}"
                                  IsChecked="{Binding IsVisible, Mode=TwoWay}"
                                  Margin="0,4,0,4" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,8">
                <Button x:Name="SelectAllButton"
                        Width="100"
                        Margin="0,0,8,0"
                        Content="Select all"
                        Click="SelectAllButton_Click" />
                <Button x:Name="UnselectAllButton"
                        Width="100"
                        Content="Unselect all"
                        Click="UnselectAllButton_Click" />
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ApplyButton"
                        Width="100"
                        Margin="0,0,8,0"
                        Content="Apply"
                        Click="ApplyButton_Click" />
                <Button x:Name="CancelButtonControl"
                        Width="100"
                        Content="Cancel"
                        Click="CancelButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
