<Window x:Class="TbotUltra.Desktop.DashboardFunctionListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="{DynamicResource AppBackgroundBrush}"
        Title="Loop Task List"
        Height="420"
        Width="380"
        MinHeight="340"
        MinWidth="340"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Select loop tasks to show and run"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,10" />

        <Border Grid.Row="1"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Background="{DynamicResource SurfaceBrush}"
                Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="FunctionOptionsListBox"
                              Background="Transparent">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Label}"
                                      IsChecked="{Binding IsVisible, Mode=TwoWay}"
                                      IsEnabled="{Binding IsSelectable}"
                                      Margin="0,4,0,4" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Vertical" HorizontalAlignment="Left" Margin="0,0,0,8">
                <Button x:Name="SelectAllButton"
                        Width="100"
                        Margin="0,0,8,0"
                        Content="Select all"
                        Click="SelectAllButton_Click" />
                <Button x:Name="UnselectAllButton"
                        Width="100"
                        Margin="0,8,8,0"
                        Content="Unselect all"
                        Click="UnselectAllButton_Click" />
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ApplyButton"
                        Width="100"
                        Margin="0,0,8,0"
                        Content="Apply"
                        Click="ApplyButton_Click" />
                <Button x:Name="CancelButtonControl"
                        Width="100"
                        Content="Cancel"
                        Click="CancelButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
