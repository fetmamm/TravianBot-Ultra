<Window x:Class="TbotUltra.Desktop.ReinforcementTroopSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="{DynamicResource AppBackgroundBrush}"
        Title="Choose troops"
        Height="540"
        Width="620"
        MinHeight="500"
        MinWidth="600"
        WindowStartupLocation="CenterOwner">
    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderSubtleBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12"
            Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Choose troops"
                           FontSize="17"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                    <TextBlock x:Name="VillageNameTextBlock"
                           Foreground="{DynamicResource TextSubtleBrush}"
                           FontSize="12"
                           Margin="0,1,0,0" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1"
                    BorderBrush="{DynamicResource BorderSubtleBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="TroopRulesItemsControl">
                        <ItemsControl.Template>
                            <ControlTemplate>
                                <ItemsPresenter />
                            </ControlTemplate>
                        </ItemsControl.Template>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid MinHeight="31" Margin="8,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="106" />
                                        <ColumnDefinition Width="64" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0"
                                              IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding TroopType}"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               Margin="8,0,4,0"
                                               VerticalAlignment="Center" />
                                    <ComboBox Grid.Column="2"
                                              Height="24"
                                              SelectedValuePath="Tag"
                                              SelectedValue="{Binding AmountMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="Fixed" Tag="fixed" />
                                        <ComboBoxItem Content="All" Tag="all_available" />
                                        <ComboBoxItem Content="20%" Tag="percent_20" />
                                        <ComboBoxItem Content="50%" Tag="percent_50" />
                                        <ComboBoxItem Content="90%" Tag="percent_90" />
                                    </ComboBox>
                                    <TextBox Grid.Column="3"
                                             Height="24"
                                             Margin="6,0,0,0"
                                             Text="{Binding AmountText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                             IsEnabled="{Binding IsFixedAmountMode}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,8,0,0">
                <Button Content="Sync settings"
                        Padding="10,5"
                        Margin="0,0,12,0"
                        Click="SyncSettingsButton_Click" />
                <Button Content="Mark all"
                        Padding="10,5"
                        Margin="0,0,6,0"
                        Click="MarkAllButton_Click" />
                <Button Content="Unmark all"
                        Padding="10,5"
                        Margin="0,0,12,0"
                        Click="UnmarkAllButton_Click" />
                <Button Content="Save"
                        Width="88"
                        Padding="10,5"
                        Background="{DynamicResource AccentBrush}"
                        Foreground="White"
                        BorderBrush="{DynamicResource AccentBrush}"
                        Click="DoneButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
