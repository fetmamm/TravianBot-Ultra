<UserControl x:Class="TbotUltra.Desktop.Views.LobbyWorldSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="500">
    <StackPanel>
        <TextBlock Text="Choose your game world"
                   FontSize="17"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}" />
        <TextBlock Margin="0,5,0,0"
                   Text="These worlds belong to your Travian lobby account. Select the correct one and Tbot Ultra will update Manage automatically after login is verified."
                   TextWrapping="Wrap"
                   LineHeight="20"
                   Foreground="{DynamicResource TextSecondaryBrush}" />

        <Border Margin="0,14,0,0"
                Padding="11,9"
                CornerRadius="7"
                Background="{DynamicResource WarningBgBrush}"
                BorderBrush="{DynamicResource WarningBorderBrush}"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="CURRENTLY CONFIGURED"
                           FontSize="10"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource WarningTextBrush}" />
                <TextBlock Text="{Binding ConfiguredTarget}"
                           Margin="0,3,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextSecondaryBrush}" />
            </StackPanel>
        </Border>

        <TextBlock Text="{Binding SelectionHint}"
                   Margin="0,12,0,8"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource TextMutedBrush}"
                   FontSize="12" />

        <ListBox x:Name="WorldListBox"
                 ItemsSource="{Binding Worlds}"
                 SelectedIndex="0"
                 MinHeight="110"
                 MaxHeight="330"
                 Background="Transparent"
                 BorderThickness="0"
                 Padding="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Cursor" Value="Hand" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="13,11"
                            CornerRadius="8"
                            BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                 Value="True">
                                        <Setter Property="Background" Value="{DynamicResource SuccessBgBrush}" />
                                        <Setter Property="BorderBrush" Value="{DynamicResource SuccessBorderBrush}" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                 Value="True">
                                        <Setter Property="BorderBrush" Value="{DynamicResource SuccessTextBrush}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextPrimaryBrush}" />
                                <TextBlock Text="{Binding Details}"
                                           Margin="0,5,12,0"
                                           TextWrapping="Wrap"
                                           LineHeight="18"
                                           Foreground="{DynamicResource TextSecondaryBrush}" />
                            </StackPanel>
                            <Border Grid.Column="1"
                                    Padding="7,3"
                                    CornerRadius="5"
                                    VerticalAlignment="Top"
                                    Background="{DynamicResource SuccessBgBrush}"
                                    BorderBrush="{DynamicResource SuccessBorderBrush}"
                                    BorderThickness="1">
                                <TextBlock Text="OWNED"
                                           FontSize="9"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource SuccessTextBrush}" />
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</UserControl>
