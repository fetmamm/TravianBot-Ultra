<Window x:Class="TbotUltra.Desktop.ConstructFasterSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="{DynamicResource AppBackgroundBrush}"
        Title="Construct 25% faster settings"
        Width="500"
        MinHeight="520"
        Height="680"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
            <TextBlock Text="Construct 25% faster by village"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       VerticalAlignment="Center" />
            <ContentControl Style="{DynamicResource InfoTooltipIconStyle}"
                            Margin="8,2,0,0"
                            ToolTip="Enable the Official Travian construct-faster bonus video per village and set duration/random gates." />
        </StackPanel>

        <Button Grid.Row="1"
                Width="92"
                Height="28"
                HorizontalAlignment="Left"
                Margin="0,0,0,8"
                Content="Toggle all"
                Click="ToggleAllButton_Click" />

        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        BorderBrush="{DynamicResource BorderSubtleBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="10,7,10,6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="54" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="1"
                                   Text="VILLAGE"
                                   FontSize="11"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextMutedBrush}" />
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Rows}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource BorderSubtleBrush}"
                                        BorderThickness="0,0,0,1"
                                        Padding="10,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="54" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <CheckBox Style="{DynamicResource ToggleSwitchPurpleStyle}"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  ToolTip="Allow construct-faster bonus videos in this village."
                                                  IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding VillageName}"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"
                                                   Margin="0,0,12,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="FontWeight" Value="SemiBold" />
                                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCapital}" Value="True">
                                                            <Setter Property="Foreground" Value="{DynamicResource CapitalBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Row="3"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="10"
                Margin="0,12,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="24" />
                </Grid.ColumnDefinitions>

                <CheckBox x:Name="MinimumBuildTimeEnabledCheckBox"
                          Grid.Row="0"
                          Grid.Column="0"
                          Content="Min build time"
                          Style="{DynamicResource ToggleSwitchPurpleStyle}"
                          Foreground="{DynamicResource TextPrimaryBrush}"
                          VerticalAlignment="Center"
                          Checked="MinimumBuildTimeToggle_Changed"
                          Unchecked="MinimumBuildTimeToggle_Changed" />

                <TextBox x:Name="MinimumBuildMinutesTextBox"
                         Grid.Row="0"
                         Grid.Column="1"
                         Width="86"
                         Padding="6,3"
                         HorizontalAlignment="Left"
                         BorderBrush="{DynamicResource BorderSubtleBrush}" />
                <ContentControl Grid.Row="0"
                                Grid.Column="2"
                                Style="{DynamicResource InfoTooltipIconStyle}"
                                Margin="5,0,0,0"
                                ToolTip="When enabled the function is used for all buildings that take longer than the value.&#10;&#10;Example: if 30 min is set and a building takes 35 min, the function will be used." />

                <CheckBox x:Name="RandomEnabledCheckBox"
                          Grid.Row="1"
                          Grid.Column="0"
                          Content="Random"
                          Style="{DynamicResource ToggleSwitchPurpleStyle}"
                          Foreground="{DynamicResource TextPrimaryBrush}"
                          Margin="0,10,0,0"
                          VerticalAlignment="Center"
                          Checked="RandomGateToggle_Changed"
                          Unchecked="RandomGateToggle_Changed"
                            />
                <ComboBox x:Name="RandomChanceComboBox"
                          Grid.Row="1"
                          Grid.Column="1"
                          Width="92"
                          Height="28"
                          Margin="0,10,0,0"
                          HorizontalAlignment="Left">
                    <ComboBoxItem Content="0%" Tag="0" />
                    <ComboBoxItem Content="10%" Tag="10" />
                    <ComboBoxItem Content="20%" Tag="20" />
                    <ComboBoxItem Content="30%" Tag="30" />
                    <ComboBoxItem Content="40%" Tag="40" />
                    <ComboBoxItem Content="50%" Tag="50" />
                    <ComboBoxItem Content="60%" Tag="60" />
                    <ComboBoxItem Content="70%" Tag="70" />
                    <ComboBoxItem Content="80%" Tag="80" />
                    <ComboBoxItem Content="90%" Tag="90" />
                    <ComboBoxItem Content="100%" Tag="100" />
                </ComboBox>
                <ContentControl Grid.Row="1"
                                Grid.Column="2"
                                Style="{DynamicResource InfoTooltipIconStyle}"
                                Margin="5,10,0,0"
                                ToolTip="Used only when Random is enabled.&#10;&#10;80% means an 80% chance to use the function before each construction." />
            </Grid>
        </Border>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Width="80"
                    Height="30"
                    Content="Cancel"
                    Margin="0,0,8,0"
                    Click="CancelButton_Click" />
            <Button Width="120"
                    Height="30"
                    Content="Save &amp; close"
                    Click="SaveButton_Click"
                    Background="{DynamicResource PrimaryButtonBrush}"
                    BorderBrush="{DynamicResource AccentBrush}"
                    Foreground="{DynamicResource OnAccentBrush}" />
        </StackPanel>
    </Grid>
</Window>
