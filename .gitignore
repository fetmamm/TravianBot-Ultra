# --- Secrets / accounts (never commit) ---
.env
config/accounts.json
/config/accounts

# --- Per-account / runtime working data (keep the repo empty for new users) ---
config/bot.json
config/queue.json
config/queue.json.lock
config/servers.user.json
config/account-analysis/
config/cache/

# --- Python (captcha trainer) ---
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Captcha training data (dev-only, not needed at runtime; kept on disk, not tracked)
Captcha_solver/math_ai/dataset/
Captcha_solver/math_ai/auto_dataset/
Captcha_solver/math_ai/test_images/
Captcha_solver/math_ai/model_backups/

# --- Playwright / browser auth ---
playwright/.auth/
ms-playwright/

# --- Build output / logs / temp / IDE ---
**/bin/
**/obj/
logs/
temp_build_out/
artifacts/
.artifacts/
.vs/
**/debug_output/
**/dataset_quarantine/
**/review_dataset/

# --- Claude tooling ---
/.claude

# --- Other
docs/CODING_FETMAMM.md
